// Este archivo se ejecuta automáticamente antes de cada test.
// Importa las utilidades extra de jest-dom, que nos permiten
// usar cosas como "expect(element).toBeInTheDocument()"

import '@testing-library/jest-dom'